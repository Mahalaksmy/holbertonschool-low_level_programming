#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int Alpha = 48,
Alpha2 = 97;

while (Alpha <= 57)
{
putchar(Alpha);
Alpha++;
}
while (Alpha2 <= 102)
{
putchar(Alpha2);
Alpha2++;
}
putchar ('\n');
return (0);
}
